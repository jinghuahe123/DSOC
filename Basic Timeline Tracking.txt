13/01/2026:
- wrote project aim
- project context and research (almost complete)
- interface with outlook COM api
- basic code that outputs all events for a day

19/01/2026:
- made gantt chart
- added json parsing for user input device name and if-correct-device-for-displaying-notification checks
- basic code that outputs all relevant events for a day




TODO:
- add message caching - save previous appointments to json file, poll every time program is run. if new calendar not equal to previous calendar send notification
- add notification UI (e.g. full webpage popup UI listing next appointments in order)
- add notification action (i.e. trigger page on boot if new messages, poll every updateTime seconds and trigger page if either new appointment created since last poll or <=reminderTime)
  - feature (and first feature) req. checking outlook every >= updateTime (if pc shut off and booted again, time will be more than updateTime anyway, no need to create special logic)

- add automatic categorising (optimisation feature) (i.e. get category from outlook as well, input json set msg description vs category override priority, input json map each category to a certain device name)
  - could also be implemented as automatic keyword categorisation?